# V14.2 Pattern Visualizer Fix

Fixes:

- Restores the missing `PatternVisualizer.drawFibonacciLevels()` method.
- Adds defensive validation for malformed Fibonacci data and zero price ranges.
- Draws 0.236, 0.382, 0.500, 0.618 and 0.786 retracement levels.
- Highlights the 0.618 golden-ratio level.
- Uses UTF-8 labels and adaptive price formatting.
- Ensures canvas state and line dashes are always restored.

The runtime error fixed is:

```text
TypeError: this.drawFibonacciLevels is not a function
```
